# Dependencies for Overmind GUI Web
sanic
pywebview
psutil

# Optional testing dependencies (for integration tests)
# Install with: pip install playwright selenium aiohttp requests
# playwright
# selenium
# aiohttp
# requests